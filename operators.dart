import 'dart:io';

main() {
  // var age;
  // var firstName;
  // int ageNum = age ??= 10;
  // print(ageNum);
  // print(age);
  // String name = firstName ?? 'Jean';
  // print(name);
  // // ternary operatiors
  // String lastName = name == 'Jean' ? 'IRADUKUNDA' : 'Kamana';
  // print(lastName);
  // calculator
  stdout.write('write the value of x');
  var x = stdin.readLineSync();
  int val = int.parse(x);
  print(val);
}
